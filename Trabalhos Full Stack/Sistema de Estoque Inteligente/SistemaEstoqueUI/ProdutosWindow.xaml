<Window x:Class="SistemaEstoqueUI.ProdutosWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Produtos"
        Height="520" Width="720"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        ResizeMode="NoResize"
        AllowsTransparency="True"
        Background="Transparent">

    <Window.Resources>
        <Style x:Key="BlueRoundedButton" TargetType="Button">
            <Setter Property="Background" Value="#4A90E2"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderBrush" Value="#4A90E2"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="6"/>
            <Setter Property="Height" Value="42"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="12"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#357ABD"/>
                    <Setter Property="BorderBrush" Value="#357ABD"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#2B5A91"/>
                    <Setter Property="BorderBrush" Value="#2B5A91"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Border Background="#EEF1F4" CornerRadius="14" Padding="18">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Botão fechar -->
            <Button Content="×"
                    Width="34" Height="34"
                    FontSize="18"
                    FontWeight="Bold"
                    Foreground="#607080"
                    Background="Transparent"
                    BorderThickness="0"
                    Cursor="Hand"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Top"
                    Margin="0 0 0 12"
                    Click="BtnFechar_Click"/>

            <TextBlock Text="Produtos"
                       FontSize="28"
                       FontWeight="Bold"
                       FontFamily="Segoe UI"
                       Foreground="#1E2A38"
                       Margin="0 40 0 12"
                       Grid.Row="0"/>

            <DataGrid x:Name="dataGridProdutos"
                      Grid.Row="1"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      IsReadOnly="True"
                      SelectionMode="Single"
                      Margin="0 0 0 12"
                      Background="White"
                      BorderBrush="#CAD9E6"
                      BorderThickness="1"
                      RowHeight="32">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding ProdutoId}" Width="50"/>
                    <DataGridTextColumn Header="Nome" Binding="{Binding Nome}" Width="*"/>
                    <DataGridTextColumn Header="Categoria" Binding="{Binding Categoria.Nome}" Width="120"/>
                    <DataGridTextColumn Header="Preço" Binding="{Binding Preco, StringFormat={}{0:C2}}" Width="90"/>
                    <DataGridTextColumn Header="Quantidade" Binding="{Binding QuantidadeEstoque}" Width="90"/>
                </DataGrid.Columns>

            </DataGrid>

            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Adicionar" Style="{StaticResource BlueRoundedButton}" Width="110" Click="BtnAdicionar_Click"/>
                <Button Content="Editar" Style="{StaticResource BlueRoundedButton}" Width="110" Click="BtnEditar_Click"/>
                <Button Content="Excluir" Style="{StaticResource BlueRoundedButton}" Width="110" Click="BtnExcluir_Click"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
