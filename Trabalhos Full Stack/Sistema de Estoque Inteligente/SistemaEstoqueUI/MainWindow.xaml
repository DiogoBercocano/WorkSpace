<Window x:Class="SistemaEstoqueUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Sistema de Estoque"
        Height="520" Width="650"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="None"
        Background="#EEF1F4"
        AllowsTransparency="True">

    <Window.Resources>
        <!-- Estilo para botões arredondados azuis -->
        <Style x:Key="BlueRoundedButton" TargetType="Button">
            <Setter Property="Background" Value="#4A90E2"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Height" Value="54"/>
            <Setter Property="Margin" Value="8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="13"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="12,0"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#2D70D5"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#1B4E9B"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Background="Transparent">

        <Border CornerRadius="22"
                Background="#F8FAFC"
                Padding="32"
                HorizontalAlignment="Center"
                VerticalAlignment="Center">
            <Border.Effect>
                <DropShadowEffect Color="#202A44" BlurRadius="32" ShadowDepth="0" Opacity="0.22"/>
            </Border.Effect>

            <StackPanel>
                <StackPanel Margin="0 0 0 8" HorizontalAlignment="Center">
                    <Ellipse Width="66" Height="66" Fill="#e3eaf2" Margin="0 0 0 10">
                        <Ellipse.Stroke>
                            <SolidColorBrush Color="#CBD5E1"/>
                        </Ellipse.Stroke>
                        <Ellipse.OpacityMask>
                            <VisualBrush>
                                <VisualBrush.Visual>
                                    <TextBlock FontFamily="Segoe MDL2 Assets"
                                               Text="&#xE77B;" FontSize="38"
                                               Foreground="#4A90E2" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                </VisualBrush.Visual>
                            </VisualBrush>
                        </Ellipse.OpacityMask>
                    </Ellipse>
                </StackPanel>
                <TextBlock Text="Painel de Controle"
                           FontSize="23"
                           FontWeight="Bold"
                           FontFamily="Segoe UI"
                           Foreground="#1E2A38"
                           HorizontalAlignment="Center"
                           Margin="0 0 0 2"/>
                <TextBlock Text="Bem-vindo ao sistema de estoque"
                           FontSize="14"
                           FontFamily="Segoe UI"
                           Foreground="#6B7C8C"
                           HorizontalAlignment="Center"
                           Margin="0 0 0 22"/>

                <UniformGrid Columns="2" Rows="2" Margin="0 0 0 6" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <Button Content="Produtos" Style="{StaticResource BlueRoundedButton}" Click="BtnProdutos_Click" />
                    <Button Content="Categorias" Style="{StaticResource BlueRoundedButton}" Click="BtnCategorias_Click" />
                    <Button Content="Fornecedores" Style="{StaticResource BlueRoundedButton}" Click="BtnFornecedores_Click" />
                    <Button Content="Movimentações" Style="{StaticResource BlueRoundedButton}" Click="BtnMovimentacoes_Click" />
                </UniformGrid>

                <TextBlock Text="Selecione uma opção para começar"
                           FontSize="13"
                           Foreground="#7A8E9C"
                           HorizontalAlignment="Center"
                           Margin="0 8 0 0" />
            </StackPanel>
        </Border>

        <!-- Botão fechar -->
        <Button Content="×"
                Width="34" Height="34"
                FontSize="18"
                FontWeight="Black"
                Foreground="#607080"
                Background="Transparent"
                BorderThickness="0"
                Cursor="Hand"
                HorizontalAlignment="Right"
                VerticalAlignment="Top"
                Margin="0 10 10 0"
                Click="BtnFechar_Click"/>
    </Grid>
</Window>
