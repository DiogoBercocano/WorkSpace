<Window x:Class="SistemaEstoqueUI.CategoriaFormWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Cadastro de Categoria"
        Height="217" Width="450"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="None"
        Background="#EEF1F4">

    <Window.Resources>
        <Style x:Key="RoundedBlueButton" TargetType="Button">
            <Setter Property="Background" Value="#4A90E2"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#4A90E2"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Height" Value="38"/>
            <Setter Property="Width" Value="110"/>
            <Setter Property="Margin" Value="6"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border CornerRadius="12" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="GrayButton" TargetType="Button" BasedOn="{StaticResource RoundedBlueButton}">
            <Setter Property="Background" Value="#6B6B6B"/>
            <Setter Property="BorderBrush" Value="#565656"/>
        </Style>
    </Window.Resources>

    <Border Background="White" Padding="25" CornerRadius="16" Margin="10" BorderBrush="#CBD5E1" BorderThickness="1.5">
        <StackPanel>
            <TextBlock Text="Nome da Categoria"
                       FontSize="16"
                       FontWeight="Bold"
                       Foreground="#1E2A38"
                       Margin="0 0 0 12" />

            <Border CornerRadius="8" Background="#F7FAFC" BorderBrush="#A0AEC0" BorderThickness="1.5" Height="40">
                <TextBox x:Name="txtNome"
                         FontSize="14"
                         Background="Transparent"
                         BorderThickness="0"
                         Padding="10"
                         VerticalContentAlignment="Center"/>
            </Border>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0 18 0 0">
                <Button Content="Cancelar" Style="{StaticResource GrayButton}" Click="BtnCancelar_Click"/>
                <Button Content="Salvar" Style="{StaticResource RoundedBlueButton}" Click="BtnSalvar_Click"/>
            </StackPanel>
        </StackPanel>
    </Border>
</Window>
