<Window x:Class="SistemaEstoqueUI.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login"
        Height="420" Width="420"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="None"
        Background="#EEF1F4"
        AllowsTransparency="True">

    <Grid Background="Transparent">

        <!-- Cartão central -->
        <Border CornerRadius="20"
                Background="#F8FAFC"
                Padding="32"
                HorizontalAlignment="Center"
                VerticalAlignment="Center">
            <Border.Effect>
                <DropShadowEffect Color="#202A44" BlurRadius="25" ShadowDepth="0" Opacity="0.25"/>
            </Border.Effect>

            <StackPanel>
                <!-- Título -->
                <TextBlock Text="Bem-vindo de volta"
                           FontSize="24"
                           FontWeight="Bold"
                           FontFamily="Segoe UI"
                           Foreground="#1E2A38"
                           HorizontalAlignment="Center"
                           Margin="0 0 0 4"/>
                <TextBlock Text="Acesse o sistema de estoque"
                           FontSize="14"
                           FontFamily="Segoe UI"
                           Foreground="#6B7C8C"
                           HorizontalAlignment="Center"
                           Margin="0 0 0 28"/>

                <!-- Usuário -->
                <StackPanel Margin="0 0 0 16">
                    <TextBlock Text="Usuário"
                               FontSize="13"
                               FontWeight="SemiBold"
                               Foreground="#506070"
                               Margin="4 0"/>
                    <Border CornerRadius="8" Background="#FFFFFF" BorderBrush="#CBD5E1" BorderThickness="1.3">
                        <TextBox x:Name="txtUsuario"
                                 Height="42"
                                 FontSize="15"
                                 Padding="10"
                                 Background="Transparent"
                                 BorderThickness="0"
                                 Foreground="#1E2A38"
                                 CaretBrush="#4A90E2"/>
                    </Border>
                </StackPanel>

                <!-- Senha -->
                <StackPanel Margin="0 0 0 28">
                    <TextBlock Text="Senha"
                               FontSize="13"
                               FontWeight="SemiBold"
                               Foreground="#506070"
                               Margin="4 0"/>
                    <Border CornerRadius="8" Background="#FFFFFF" BorderBrush="#CBD5E1" BorderThickness="1.3">
                        <PasswordBox x:Name="txtSenha"
                                     Height="42"
                                     FontSize="15"
                                     Padding="10"
                                     Background="Transparent"
                                     BorderThickness="0"
                                     Foreground="#1E2A38"
                                     CaretBrush="#4A90E2"/>
                    </Border>
                </StackPanel>

                <!-- Botão -->
                <Border CornerRadius="8" Margin="0 0 0 12">
                    <Button Content="Entrar"
                            Height="42"
                            FontSize="16"
                            FontWeight="Bold"
                            Cursor="Hand"
                            BorderThickness="0"
                            Background="#4A90E2"
                            Foreground="White"
                            Click="BtnEntrar_Click">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Background" Value="#4A90E2"/>
                                <Setter Property="Foreground" Value="White"/>
                                <Setter Property="SnapsToDevicePixels" Value="True"/>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#5BA2F0"/>
                                        <Setter Property="Cursor" Value="Hand"/>
                                    </Trigger>
                                    <Trigger Property="IsPressed" Value="True">
                                        <Setter Property="Background" Value="#397DD1"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </Border>

                <!-- Link Esqueci Senha -->
                <TextBlock Text="Esqueceu a senha?"
                           Foreground="#4A90E2"
                           FontSize="13"
                           HorizontalAlignment="Center"
                           Cursor="Hand"
                           TextDecorations="Underline"/>
            </StackPanel>
        </Border>

        <!-- Botão fechar -->
        <Button Content="×"
                Width="34" Height="34"
                FontSize="18"
                FontWeight="Bold"
                Foreground="#607080"
                Background="Transparent"
                BorderThickness="0"
                Cursor="Hand"
                HorizontalAlignment="Right"
                VerticalAlignment="Top"
                Margin="0 10 10 0"
                Click="BtnFechar_Click"/>
    </Grid>
</Window>
